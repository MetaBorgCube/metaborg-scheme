module dynamic-semantics

rules
  map[term -> instr]:: term -> instr 
  map[X]([e]) =
    X(e)
    
  map[X]([h|t]) =
    X(h);
    map[X](t)


  eval:: term -> instr
  eval(Program(exps)) = 
  	map[eval-top](exps)