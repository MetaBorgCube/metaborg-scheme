module dynamic-semantics

imports
  top-exp
  
signature
  link-labels
    P
  continuations
    ret
  namespaces
    Var

rules
  eval:: ast -> instr
  eval(Program(exps)) = 
  	map[eval-top](exps);
  	exit(int(0))